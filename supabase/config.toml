project_id = "asolssebjyjyfbggraew"

# Public callbacks invoked by the AI Music Studio engine — no user JWT.
# Both endpoints validate a signed token bound to the generation job.
[functions.ai-music-studio-lyrics-callback]
verify_jwt = false

[functions.ai-music-studio-music-callback]
verify_jwt = false

# Invited users may have no session yet (email confirmation pending) when
# accepting; the function validates the signed invitation token itself.
[functions.accept-invitation]
verify_jwt = false
